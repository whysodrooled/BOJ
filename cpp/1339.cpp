#include <bits/stdc++.h>
typedef long long ll;

using namespace std;

map<char, int> counts;
int N;
string strs[10];

int main()
{
    cin.tie(NULL);
    cout.tie(NULL);
    ios_base::sync_with_stdio(false);

    for (size_t i = 0; i < N; i++)
    {
        cin >> strs[i];
        
        for (size_t j = strs[i].size()-1; j >= 0; j--)
        {
            /* code */
        }
        
    }
    

    return 0;
}